start project
    docker-compose up
    docker-compose up --build
stop project
    docker-compose stop

web
    http://localhost:81

mysql:
    login root
    pass bfigf_iee-bifjb

docker login:
    docker exec -it kyrs_php /bin/bash
    docker exec -it kyrs_php php artisan migrate
    docker exec -it kyrs_php php artisan db:seed
    docker exec -it kyrs_php php artisan key:generate

Run test
    docker exec -it kyrs_php phpunit
    docker exec -it kyrs_php phpunit --coverage-html ./coverage
